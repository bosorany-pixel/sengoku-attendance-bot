# API endpoint for production
# For local testing with preview mode, point directly to backend
VITE_API_URL=http://localhost:8000/api

# For actual production deployment with reverse proxy, use:
# VITE_API_URL=/api
